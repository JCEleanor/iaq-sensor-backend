## Meeting notes

#### Backend implementation

1. A deployment table is needed in the future. Begin with the simplest design and draw an ERD to clarify relationship
2. The first data_counter would be 0. TBD on reset, but it could be persistent bc of NVRAM
3. use REAL for all sensor data
4. 2 endpoints for committing data: `/hello` for the first time (data_counter = 0), `/insert` for the following data
5. some test cases to cover during testing:
   - insert data into already compressed chuck

### Benchmarking

Define KPI for performance / Things to consider:

- write latency:
  - on the server (server processing time)
    - insert data into already compressed chuck
  - e2e on client (network time etc)
- offered load
- tail latency
- number of sensors
- duration of test
- distribution for sensor/network factors

## Eleanor's TODO

1. complete the happy flow (`/hello` & `/insert`)
2. ERD
3. change sensor data to REAL
4. migrate to new github repo
5. study prometheus
